export interface ISvgProps {
    className?: string;
    color?: string;
    width?: number;
    height?: number;
    outlineColor?: string;
    // id: string; const id = useId().replace(/:/g, '');
}
