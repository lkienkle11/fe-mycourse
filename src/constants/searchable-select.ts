/** Default page size for searchable dropdown infinite scroll (BE max per_page is 100). */
export const SEARCHABLE_SELECT_PER_PAGE = 20;

/** Debounce delay before applying search text to paginated fetchers. */
export const SEARCHABLE_SELECT_SEARCH_DEBOUNCE_MS = 300;

/** Scroll threshold (px from bottom) to trigger loading the next page. */
export const SEARCHABLE_SELECT_LOAD_MORE_THRESHOLD_PX = 48;
