/** Responsive max width for admin instructor profile / application view dialogs. */
export const INSTRUCTOR_PROFILE_DIALOG_MAX_WIDTH_CLASS =
  "w-[calc(100%-2rem)] sm:max-w-3xl lg:max-w-4xl";

/** Query param on `/admin|sysadmin/instructors/roster` — instructor `user_id` (UUID). */
export const INSTRUCTOR_ROSTER_PORTFOLIO_ID_PARAM = "portfolioId";
