# Session Summary

> Saved: 2026-05-19 12:00:00
> Branch: feat/stream-api-v1 (FE) / docs/update-plan-v1 (BE)
> Project: fe-mycourse + be-mycourse (register/confirm flow)

## Overview
Baseline for implementing FE register + email confirm flow aligned with BE `POST /auth/register` and `POST /auth/confirm`. BE already uses `APP_CLIENT_BASE_URL` but confirmation links still point to `/api/v1/auth/confirm?token=`. Plan requires `/{locale}/confirm-email?token=` and full FE wiring.

## Inventory gap (pre-implementation)

| Layer | Gap |
|-------|-----|
| BE | `RegisterRequest` has no `locale`; confirm URL L213 is API path not FE path |
| FE | `signupAction` placeholder; API route `signup` vs BE `register`; no confirm page; missing error codes 4009–4011 |
| FE | No `.context/` folder; no `.gitnexus/` index yet |

## Key decisions (from plan)
- Email link: `{APP_CLIENT_BASE_URL}/{locale}/confirm-email?token={uuid}`; locale from register body (`en`|`vi`, default `vi`)
- FE confirm page calls `POST /api/v1/auth/confirm` with JSON `{ token }`; shared cookie util with login
- BE role `learner` on confirm documented but not implemented in code — no FE dependency

## Files to touch (planned)
- BE: `dto.go`, `handler.go`, `service.go`, `docs/modules/auth.md`, curl/api docs
- FE: `api-route.ts`, `route.ts`, `types/api.ts`, `schema/auth`, `api/callers/auth`, `actions/auth`, signup/login components, `confirm-email/page.tsx`, i18n, docs

## Next steps
1. Todo 1 — BE locale + email URL
2. Todo 2–5 — FE foundation, register modal, confirm page, login polish
3. Todo 6 — verification, docs, GitNexus analyze both repos

## Blockers
- GitNexus `analyze --force` on FE failed in sandbox (EPERM on `~/.gitnexus/registry.json`) — retry with full permissions
