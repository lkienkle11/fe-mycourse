# Session: FE UI & DnD libraries install

**Date:** 2026-05-20

## Scope

Implemented plan phases 0–5: shadcn Radix batch, typography, DnD npm packages, barrel exports, quality gate, docs.

## Base UI policy

| Check | Result |
|-------|--------|
| Phase 0 baseline (`@base-ui`, `combobox.tsx`) | **Clean** — zero hits |
| Phase 1 post-install | **PASS** — no `@base-ui/*`, no `combobox.tsx` |
| Rollback required? | **No** |

**Forbidden:** `shadcn add combobox`, `@base-ui/react`. **Substitute:** `select` + `popover`.

## Installed (shadcn@4.2.0, `--overwrite -y`)

`sidebar`, `calendar`, `chart`, `carousel`, `drawer`, `empty`, `resizable`, `switch`, `tooltip`, `hover-card`, `accordion`, `breadcrumb`, `radio-group`, `select`, `popover` + `radix-ui` npm.

**Skipped (already present):** `badge`, `checkbox`.

**Overwritten:** `button.tsx`, `input.tsx`, `separator.tsx` (+ `chart`/`drawer`/`empty`/`resizable` refreshed).

**New files:** 18 shadcn components + `src/hooks/use-mobile.ts` + manual `typography.tsx`.

## npm packages added

- `radix-ui`, `date-fns`, `react-day-picker`, `embla-carousel-react`, `recharts`, `react-resizable-panels`, `vaul`
- `@dnd-kit/core`, `sortable`, `utilities`, `modifiers`, `accessibility`
- `@nosferatu500/react-sortable-tree`, `react-dnd`, `react-dnd-html5-backend`

## Fixes applied

- `calendar.tsx`: `table` → `month_grid` (react-day-picker v10 `ClassNames` API)
- `biome.json`: override for `src/components/ui/**` (shadcn upstream a11y/chart patterns)

## Barrel exports

- `src/components/ui/index.ts` — all 31 UI modules
- `src/hooks/index.ts` — `useIsMobile` from `use-mobile.ts`

## Quality gate

| Command | Result |
|---------|--------|
| `npm run format:biome` | ✅ |
| `npm run lint:biome` | ✅ (5 warnings in ui — shadcn upstream) |
| `npm run lint` (eslint) | ✅ |
| `npx tsc --noEmit` | ✅ |
| `npm run build` | ✅ |

## GitNexus (fe-mycourse)

- `npx gitnexus analyze --force` — 1,374 nodes, 68 flows
- `detect_changes({ scope: "all" })` — **LOW** risk; changed: Button, Input, Separator, index barrels; 0 affected processes

## Docs updated

- `docs/components.md` — full UI inventory + DnD note
- `docs/dependencies.md` — new packages + Base UI policy
- `docs/architecture.md` — stack + Ui cluster list

## Phase 7 (2026-05-20, complete catalog)

Installed **20** shadcn components: `alert`, `alert-dialog`, `aspect-ratio`, `button-group`, `collapsible`, `command`, `context-menu`, `direction`, `input-otp`, `item`, `kbd`, `menubar`, `navigation-menu`, `progress`, `slider`, `spinner`, `table`, `tabs`, `toggle`, `toggle-group`.

- New deps: `cmdk`, `input-otp`
- Overwritten: `button`, `separator`, `input`, `textarea`, `dialog`, `input-group`
- Base UI scan: **PASS**
- Total UI files: **54** (+ `index.ts` with 54 exports)
- Skipped: `combobox`, `data-table`, `date-picker`, `toast`, `scrollbar`; optional `ui/sonner` not installed (app uses `sonner` directly)
- Quality gate: biome, eslint, tsc, build — **pass**

## Phase 6 (2026-05-20, supplemental)

| Component | File | Stack |
|-----------|------|-------|
| `Pagination` | `pagination.tsx` | Button + Tailwind |
| `ScrollArea` | `scroll-area.tsx` | `radix-ui` (scrollbar substitute — no `scrollbar` CLI) |
| `NativeSelect` | `native-select.tsx` | Native HTML `<select>` |

- Base UI post-scan: **PASS**
- `button.tsx` overwritten (pagination dependency)
- Quality gate re-run: biome, eslint, tsc, build — **all pass**
- `ui/index.ts` exports: `pagination`, `scroll-area`, `native-select`

## Notes for future work

- Wrap app with `TooltipProvider` when using tooltips globally
- Searchable combobox: build with `Popover` + filtered list (not Base UI combobox)
- DnD/tree packages installed only — no feature wrappers yet
- **`Select` vs `NativeSelect`:** Radix custom dropdown vs OS-native `<select>` — pick per form UX
