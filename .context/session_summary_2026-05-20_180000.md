# Session: Unified mobile header sidebar (below lg)

**Date:** 2026-05-20

## Breakpoint

- **`lg` (1024px):** mobile/tablet bar (logo + burger) vs desktop full header row.
- Cart stays on desktop only.

## What shipped

### Extracted / extended

- `browse-menu-tree.tsx` — `BrowseMenuTree` (recursive Accordion from `BROWSE_MENU_ITEMS`).
- `user-menu-dropdown-items.tsx` — shared `HEADER_DROPDOWN_ITEMS` for desktop + sidebar.
- `SearchBar` — `visibility: "header" | "sidebar"`.
- `LocaleSwitcher` — `fullWidth`, `triggerClassName`, `onNavigate`.

### Mobile chrome

- `header-mobile-bar.tsx` — logo + burger.
- `header-mobile-sidebar.tsx` — left Sheet: search, browse tree, locale, auth footer.
- `sidebar-auth-footer.tsx` — guest `AuthButton` or avatar + dropdown.

### Refactors

- `header.tsx` — `hidden lg:flex` desktop row + `HeaderMobileBar`; single `LoginSignupPopup`.
- `browse-nav.tsx` — desktop-only (`lg+`); removed duplicate mobile Sheet.
- `auth-layout.tsx` — chrome only (popup moved to header).
- `user-menu.tsx` — uses `UserMenuDropdownItems`.

### i18n

- `commonHeader.menu.open`, `language`, `account` in `en.json` / `vi.json`.

## GitNexus

| Symbol | Risk |
|--------|------|
| `HeaderBrowseNav` (pre-edit) | LOW |
| `BrowseMenuTree` | LOW |
| `HeaderMobileSidebar` | LOW |
| `detect_changes` | medium (SearchBar `handleSearch` name collision in auth flows — not a real break) |

Reindexed: `npx gitnexus analyze --force` (1550 nodes).

## Quality gate

| Check | Result |
|-------|--------|
| biome format/lint | ✅ (3 pre-existing ui warnings) |
| eslint | ✅ |
| tsc | ✅ |
| build | ✅ |

## Docs updated

- `docs/screens.md`, `docs/components.md`, `docs/folder-structure.md`

## Manual test

- [ ] `< lg`: logo + burger only; sidebar has search, browse tree, locale, auth.
- [ ] `≥ lg`: full header unchanged; no second burger from browse-nav.
- [ ] Sidebar login/register opens modals.
- [ ] Locale switch closes sidebar and preserves path.
