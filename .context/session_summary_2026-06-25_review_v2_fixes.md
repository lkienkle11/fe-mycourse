# Session summary — review v2 fixes (2026-06-25)

## Changes
- `UserMultiSelectPickerFeatureDialog` + label hooks; thin roster/collaborator wrappers.
- Partial success refreshes candidate list via `onAfterPartialSuccess` + `mutate`.
- `USER_PICKER_PER_PAGE` in `src/constants/user-picker.ts`.

## Quality gates
- `npm run check-all`: PASS
