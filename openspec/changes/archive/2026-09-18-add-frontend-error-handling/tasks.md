## 1. Pre-edit impact analysis (GitNexus)

- [x] 1.1 Run `gitnexus_impact({target: "InstructorCourseEditorPage", direction: "upstream"})` before touching `src/screen/instructor/courses/editor-page.tsx` and record the blast radius (direct callers, affected processes, risk level); stop and warn before editing if the result is HIGH or CRITICAL risk.
- [x] 1.2 Run `gitnexus_context({name: "useCourseDetail"})` to confirm its current callers and returned shape (`data`/`error`/`isLoading`/`mutate`) before relying on `error` in task 5.1.

## 2. Error classification helper

- [x] 2.1 Add `classifyApiError(error): StatusErrorVariant | "unknown"` in `src/lib/utils/api-error.ts`, mapping `ApiNetworkError` → `network`, `ApiHttpError` with `response.status` 401/403/5xx → `unauthorized`/`forbidden`/`server-error`, and everything else → `unknown`; verify with unit tests covering each mapped status plus the unknown fallback. (Amended, on request: refactored the status→variant mapping from an `if`/`else` chain to an ordered `HTTP_STATUS_RULES` table — `{status, variant}` exact entries and `{minStatus, maxStatus, variant}` range entries, resolved by one `find()` — so a new status code is a new table row, not new branching logic. Also folded `ApiTimeoutError` into `network` alongside `ApiNetworkError`, verified with a new unit test, and extended the 5xx-range test to cover the 599 boundary. A `429 → rate-limited` entry was added then removed again in the same session, both on request — no 429 entry ships; verified with a test asserting 429 now falls to `"unknown"`.)
- [x] 2.2 Export the `StatusErrorVariant` type alongside `classifyApiError` and verify `npm run lint` reports no new violations on the file.

## 3. `StatusErrorPage` shared component

- [x] 3.1 Create `src/components/shared/status-error-page.tsx` (client component) accepting `variant: "unauthorized" | "forbidden" | "server-error" | "network"` plus optional `title`/`description`/`action` overrides, rendering a `lucide-react` icon + status heading + description + primary action button, generalized from `src/screen/common/not-found/not-found-page.tsx`'s layout; verify by rendering each variant in a component test asserting the expected icon and default copy. (Amended: the originally-planned `"not-found-data"` variant was removed — `classifyApiError` never produces it and no consumer set it manually, so it was dead code; a 404 primary-request response stays out of scope for this component. A `rate-limited` variant (429, `HourglassIcon`) was later added then removed again in the same session, both on request — the shipped variant set is unchanged from this task's original four.) (Further amended, on request, for visual consistency with `global-error.tsx`'s finalized card design: wrapped the icon/title/description/action in a `rounded-[1.25rem] bg-card shadow-xl` card with generous padding/gaps, using the app's `bg-card` theme token rather than hardcoded colors since — unlike `global-error.tsx` — this component always renders inside a themed layout. Added an opt-in `fillViewport` prop, default `false` (`min-h-[50vh]`, safe for the embedded `editor-page.tsx` case) vs. `true` (`min-h-[calc(100svh-4rem)]`, matching `NotFoundPage`'s convention so a page's footer sits at the bottom of the screen when this is the page's sole content) — added after live review showed the footer floating right under the card with dead space below when previewed as a full page.)
- [x] 3.2 Add the five variants' default title/description keys to `src/messages/en.ts` and `src/messages/vi.ts` under a new `errors.statusPage` namespace, matching each locale file's existing language and tone; verify `npm run test` picks up the new keys without missing-key warnings. (Amended: a `rate-limited` key pair was added then removed again in the same session, both on request — the shipped `errors.statusPage` namespace covers the original four variants only.)

## 4. Route-level error boundaries

- [x] 4.1 Add `src/app/[locale]/error.tsx` (client component) that renders `StatusErrorPage` with a generic "something went wrong" variant and a retry action calling the boundary's `reset()`; verify with a component/browser test that a component thrown error inside a nested route is caught here instead of propagating further. (Amended: a live e2e proof was tried and reverted — it needed an always-present, intentionally-throwing test route (`e2e-error-boundary-test`), which the user flagged as exposing internal test tooling/business logic through a public route, same objection as 4.2 below. Verification for this boundary is the Jest test (`src/app/[locale]/error.test.tsx`: mounts the boundary directly, asserts the fallback UI renders and `reset()` is called) plus Next.js's own documented convention that a co-located `error.tsx` automatically becomes its segment's error boundary — a framework guarantee, not app-specific behavior this app's e2e suite needs to re-prove.)
- [x] 4.2 Add `src/app/global-error.tsx` that owns its own minimal `<html>`/`<body>` (no i18n provider, no external data fetching) and renders a static branded fallback; verify with a browser test that an error thrown from the root layout renders this fallback instead of a blank page. (Amended: a live e2e proof was tried and reverted — it needed a second, locale-less test route (`e2e-global-error-test`) plus a `src/proxy.ts` middleware-matcher exclusion to route around the locale redirect, which the user flagged as exposing internal test tooling/business logic through a public route and a production routing exception. `global-error.tsx` is instead covered by `src/app/global-error.test.tsx` (Jest: renders the branded fallback, calls `reset()`) plus Next.js's own documented framework contract that `global-error.tsx` is invoked for any error escaping every segment boundary — a framework guarantee that doesn't need this app's e2e suite to re-prove it.) (Further amended: the "static branded fallback" is no longer English-only — it now reads `errors.boundary` from `src/messages/{en,vi}.ts` directly (plain object import, no `NextIntlClientProvider` needed) and detects the locale from the URL's first path segment via `useSyncExternalStore`, falling back to `routing.defaultLocale`. Verified with 3 Jest cases — default `vi`, `/en/...` path, and an unrecognized segment falling back to `vi` — and manually in a real `next dev` run via a temporary, immediately-reverted `throw` in `src/app/layout.tsx` (not a shipped route), confirming `<html lang>` and copy both switch correctly.)

## 5. Wire `editor-page.tsx` to the new classification

- [x] 5.1 Update `src/screen/instructor/courses/editor-page.tsx` to destructure `error` from `useCourseDetail`, call `classifyApiError(error)`, and render the matching `StatusErrorPage` variant before falling back to the existing generic "could not load" message for the truly-unknown case; verify with a component test asserting a 403 response renders the forbidden variant and a network failure renders the network variant, distinct from each other and from the unknown fallback. (Amended: also verified with a real e2e journey, `e2e/tests/course-error-states.spec.ts` — a non-collaborator instructor loading a fixed always-403 `course-fixture-forbidden` course sees the rendered forbidden `StatusErrorPage` and its "Back to courses" action, against a real Chromium + `next start` build. This needed no test-only route or middleware exception, unlike the boundary e2e attempts in 4.1/4.2, so it shipped.)
- [x] 5.2 Confirm no other current consumer of `useCourseDetail` regresses by running `npm run test` for the `editor-page` and related course-collaborator test suites.

## 6. Documentation sync (existing files — update, do not create new ones)

- [x] 6.1 Update `docs/router.md`: add the new `src/app/[locale]/error.tsx` and `src/app/global-error.tsx` boundaries under `## App Router Tree` / `### Route Groups`, and cross-reference them from the existing `## Custom 404 (not-found)` section so the doc distinguishes the route-not-found case from the new render-error boundaries.
- [x] 6.2 Update `docs/screens.md`: add a section for `StatusErrorPage` next to the existing `## Not Found Screen (NotFoundPage)` section, describing the five variants and when each is used; update `## GitNexus Cluster Mapping` if the new component/routes change the cluster mapping. (Amended: the shipped section documents four variants (`unauthorized`/`forbidden`/`server-error`/`network`), not five — the originally-planned `not-found-data` variant was never added, per task 3.1's amendment. No `## GitNexus Cluster Mapping` section exists in `docs/screens.md`; nothing to update there.)
- [x] 6.3 Update `docs/components.md`: list `StatusErrorPage` under `## shared/ — Cross-Feature Components`.
- [x] 6.4 Update `docs/patterns.md`: extend `## 6b. API Error Pattern (all modules)` with the new full-page pattern (`classifyApiError` + `StatusErrorPage`) alongside the existing toast-only (`toastApiError`) pattern, and state explicitly when to use which.
- [x] 6.5 Re-read all four updated docs after editing and remove or correct any sentence that now contradicts the implemented code (per this project's `AGENTS.md` Documentation and Context Sync rule) — do not leave old and new descriptions side by side.

## 7. GitNexus sync and quality gates

- [x] 7.1 Run `npx gitnexus analyze` (add `--embeddings` if `.gitnexus/meta.json` currently shows `stats.embeddings > 0`) to re-sync the index after all file changes above.
- [x] 7.2 Run `gitnexus_detect_changes({scope: "all"})` and confirm only the files listed in proposal.md's Impact section were affected.
- [x] 7.3 Run `npm run check-all` and fix any lint/type/test/build/deadcode/deps failures introduced by this change; do not report this task done unless the command actually completed successfully.

## 8. Post-implementation review and manual verification

- [x] 8.1 Review every file changed in this task list against this project's `AGENTS.md` Post-Implementation Code Review Requirement: follows existing architecture/folder structure, reuses existing utilities/components where possible, no duplicate logic introduced, no unrelated files touched, no secrets/credentials hardcoded.
- [x] 8.2 Manually exercise the golden path in the dev server (`npm run dev`): load a course as a non-collaborator instructor and confirm the forbidden `StatusErrorPage` renders instead of the old generic fallback, per the original Course-collaborator FE issue this change resolves.
