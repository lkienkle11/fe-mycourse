## 1. Pre-change impact analysis (GitNexus, per AGENTS.md)

- [x] 1.1 Check `gitnexus://repo/fe-mycourse/context` for index freshness; if stale, run `npx gitnexus analyze` (add `--embeddings` if `.gitnexus/meta.json`'s `stats.embeddings` is non-zero) before continuing — index was 2 commits behind HEAD; `stats.embeddings` was 0 so ran plain `npx gitnexus analyze` (no `--embeddings`); now 4,195 nodes / 10,423 edges / 157 clusters / 300 flows
- [x] 1.2 Run `gitnexus_impact({target: "DashboardLayout", direction: "upstream"})` and record the reported risk level and d=1 callers — GitNexus resolved 0 upstream callers (`risk: UNKNOWN`, `epistemic: lower-bound`; JSX-usage of a function component is a known blind spot for the call-graph, noted in the tool's own `riskNote`); fell back to a repo grep, which confirmed the expected 3 callers: `src/app/[locale]/instructor/layout.tsx`, `admin/layout.tsx`, `sysadmin/layout.tsx` (the last two via `RoleDashboardLayout`). No HIGH/CRITICAL risk, no unexpected caller
- [x] 1.3 Run `gitnexus_impact({target: "HeaderDashboard", direction: "upstream"})` — same 0-upstream/lower-bound result for the same JSX-usage reason; grep-confirmed `HeaderDashboard`'s only import site is `dashboard-layout.tsx` (both the authorized and denial `HeaderDashboard` calls live there), so no other caller relies on the denial branch's `leading` being empty

## 2. Implementation

- [x] 2.1 ~~In `src/components/common/dashboard/dashboard-layout.tsx`, add a local `DashboardDeniedMobileBrand` component~~ — done, then superseded by group 5 below: the user pointed out this markup duplicated `header-mobile-bar.tsx`'s existing logo-only link, so it was extracted into a shared `BrandLogoLink` instead of staying local
- [x] 2.2 Pass a logo-only leading element to the `HeaderDashboard` call in the denial branch (`!isLoading && !canAccessDashboard`, currently `dashboard-layout.tsx:203-206`); verified via `git diff` that the authorized branch's `HeaderDashboard` call (`leading={<DashboardMenuTrigger />}`) is untouched — final element is `<BrandLogoLink className="md:hidden" />` per group 5
- [x] 2.3 Manually verified via Playwright at 375×812 on `http://localhost:3000/vi/admin` (unauthenticated, `unauthorized` variant): the MyCourse logo alone (no title text) renders top-left, "Đăng nhập"/"Đăng ký" auth entry points remain top-right, matching the reported bug's exact page. The authorized branch was not touched (confirmed via diff in 2.2), and its `DashboardMenuTrigger` is unconditionally `md:hidden`, independent of this change
- [x] 2.4 Manually verified via Playwright at 1280×800 on the same route: `HeaderDashboard`'s own "MyCourses.io" logo+title button renders top-left as before, `DashboardDeniedMobileBrand` correctly stays hidden (`md:hidden`), no duplicate logo

## 3. Documentation sync (files confirmed to reference the changed behavior)

- [x] 3.1 `docs/screens.md:287` — updated the "Denied:" line (superseded by group 5 to name `BrandLogoLink` instead of `DashboardDeniedMobileBrand`)
- [x] 3.2 `docs/components.md:132` — appended to the `DashboardLayout` row (superseded by group 5 to name `BrandLogoLink` instead of `DashboardDeniedMobileBrand`)
- [x] 3.3 `docs/reusable-assets.md:367` — extended the "Layout permission gate: on denial..." sentence (superseded by group 5 to name `BrandLogoLink` instead of `DashboardDeniedMobileBrand`)
- [x] 3.4 Confirmed via grep at the time: `DashboardHeaderLocale`/`DashboardMenuTrigger` have no doc row of their own, so the original local `DashboardDeniedMobileBrand` didn't need one either — moot after group 5, since `BrandLogoLink` is now a real exported component and does get its own row (`docs/components.md`)
- [x] 3.5 Update the current project's `.context/session-<SESSION_ID>.md` with what changed and why, per AGENTS.md's end-of-task learning summary requirement — written to `.context/session-a3fd2aad-758d-4476-af71-dca2c45cc652.md`

## 5. Revision: extract shared `BrandLogoLink` (duplication caught by user)

User reviewed the group-2 implementation and pointed out that the new `DashboardDeniedMobileBrand` markup (`<Link href={homeHref} className="..."><MainLogo /></Link>`) duplicated the existing logo-only link already in `header-mobile-bar.tsx:23-25`, per this repo's Critical Deduplication Rule (AGENTS.md). This group extracts a shared component and rewires both call sites.

- [x] 5.1 Ran `gitnexus_impact({target: "HeaderMobileBar", direction: "upstream"})` before editing it — same JSX-usage 0-upstream/lower-bound result as 1.2/1.3; grep-confirmed `HeaderMobileBar`'s only caller is `header.tsx`, and only its internals change (no prop/signature change), so no risk to that caller
- [x] 5.2 Created `src/components/common/header/brand-logo-link.tsx` (`BrandLogoLink`, optional `className` prop, `cn("flex items-center select-none", className)` around `<MainLogo />`, `Link` to `homeHref`) and added it to `src/components/common/header/index.ts`'s barrel
- [x] 5.3 Updated `header-mobile-bar.tsx` to render `<BrandLogoLink />` in place of its inline `Link`/`MainLogo` markup; removed the now-unused `MainLogo`/`Link`/`homeHref` imports from that file
- [x] 5.4 Updated `dashboard-layout.tsx`: deleted the local `DashboardDeniedMobileBrand` function entirely, changed the denial branch's `HeaderDashboard` call to `leading={<BrandLogoLink className="md:hidden" />}`, added the `BrandLogoLink` import; verified `MainLogo`/`Link`/`homeHref` imports are still needed for `DashboardSidebarMobileHeader` elsewhere in the same file (they are, so they stay)
- [x] 5.5 Updated `design.md`'s Decisions section to record the reversal and why (duplication caught, not just theoretical), and `proposal.md`'s Impact section to list the new file, the two updated call sites, and the corrected doc line numbers
- [x] 5.6 Updated docs to name `BrandLogoLink` instead of `DashboardDeniedMobileBrand`: `docs/screens.md:287`, `docs/components.md` (`HeaderMobileBar` row, barrel list, `DashboardLayout` row, new `BrandLogoLink` row), `docs/reusable-assets.md:367`, plus `docs/architecture.md:126` and `docs/folder-structure.md:168-170` (now a real exported file in `header/`, unlike the deleted local helper, so the folder/barrel listings do need it)
- [x] 5.7 Manually re-verified via Playwright: 375×812 on `http://localhost:3000/vi/admin` still shows the logo-only brand mark top-left on denial; the public marketing header (`/` at `<lg` width, `HeaderMobileBar`) still shows the same logo unchanged after the refactor
- [x] 5.8 Re-ran `npm run check-all` and `openspec validate fix-dashboard-mobile-forbidden-logo --strict` after this revision; re-ran `gitnexus_detect_changes({scope: "all"})` and updated the session context file with this correction (see results in group 4, re-run after this group)

## 4. Validate (per AGENTS.md pre-commit requirements)

- [x] 4.1 Run `npm run check-all` and fix any type, lint, or test failures introduced by this change — ran twice (before and after group 5's revision); both passed clean: eslint, biome, jest (34+110 tests), knip (deadcode — correctly did not flag the deleted `DashboardDeniedMobileBrand` or the new `BrandLogoLink` barrel export as unused), madge (0 circular deps), jscpd (0 clones — confirms the extraction actually removed the duplication jscpd would otherwise have flagged), and `next build` (TypeScript + all routes compiled)
- [x] 4.2 Run `openspec validate fix-dashboard-mobile-forbidden-logo --strict` and resolve any reported issues — passed both times: "Change 'fix-dashboard-mobile-forbidden-logo' is valid"
- [x] 4.3 Ran `gitnexus_detect_changes({scope: "all"})` instead of `scope: "staged"` (nothing `git add`ed or committed yet, not asked to commit this session; `staged` would report a false-clean 0-change result) — final run (post group-5) reports 18 changed symbols / 10 files, risk `medium`: `DashboardLayout` and `HeaderMobileBar` (both expected code changes), `docs/screens.md`, `docs/components.md` (3 sections), `docs/reusable-assets.md` (3 sections), `docs/architecture.md`, `docs/folder-structure.md` (all expected doc updates), plus `AGENTS.md`/`CLAUDE.md`'s auto-updated GitNexus badge. Cross-checked against `git status --short`: exactly 10 modified files + 2 untracked (`brand-logo-link.tsx`, the openspec change dir) — matches exactly, no unexpected file. The new `BrandLogoLink` symbol itself doesn't appear in the changed-symbols list since it's a brand-new file not yet reindexed (same as `DashboardDeniedMobileBrand` didn't appear in the first pass) — not a concern, `git status`/`git diff` already confirm its content directly. `medium` risk unchanged from the first pass, same 4 `DashboardLayout`-rooted permission-check processes
- [x] 4.4 After committing, re-run `npx gitnexus analyze` (add `--embeddings` if `.gitnexus/meta.json`'s `stats.embeddings` is non-zero) to keep the index current for the next session — `stats.embeddings` was 0, ran plain `npx gitnexus analyze`; now 4,200 nodes / 10,434 edges / 158 clusters / 300 flows. This bumped `AGENTS.md`/`CLAUDE.md`'s badge again (new `BrandLogoLink` symbol now indexed), committed separately as a 5th commit (`docs: refresh GitNexus index counts`)
