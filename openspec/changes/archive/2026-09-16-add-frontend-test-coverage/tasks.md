This checklist is for the future implementation under `/opsx:apply`. Editing these planning artifacts does not execute the implementation lifecycle. Keep exactly three delivery stages; within each stage follow AGENTS.md in order: bootstrap/context and project understanding → implementation with reuse → GitNexus force-sync → quality gates → review → full documentation sync → session learning and final report. A stage cannot be marked complete merely because its tests were written.

For every implementation item below, use the approved reuse map, preserve existing architecture, and perform upstream impact before modifying an existing symbol. Do not create duplicate resources. If protected tooling cannot be satisfied, follow AGENTS.md's explain → descriptive implementation stash → await explicit decision → reapply/refactor workflow; do not suppress rules. No commit, push, branch rename, or deployment is authorized by this checklist.

## 1. Stage 1 — Foundation and reference tests

- [x] 1.1 Classify the apply task as implementation and establish or reuse the conversation bootstrap outside every project before reading project content or running implementation commands; verify `AI_SESSION_ID_FILE` contains exactly one stable ID, `AI_SESSION_ID` matches when supplied, and repair invalid bootstrap state before proceeding. **Done retroactively**: `AI_SESSION_ID_FILE=/var/folders/.../ai-agent-sessions/fe-mycourse-add-frontend-test-coverage-20260916/session-id`, `AI_SESSION_ID=8ac1e19a-e093-478b-b3e1-41d3e67ffda0`; verified the file still exists, contains exactly this one ID, and is outside every project.
- [x] 1.2 Read the complete existing project .context folder, then resolve exactly one `.context/session-<SESSION_ID>.md` for this project; verify an exact existing match is reused, absence creates only that file, and duplicate matches are resolved before continuing. Verified: exactly one `.context/session-8ac1e19a-e093-478b-b3e1-41d3e67ffda0.md` exists (16 other files belong to unrelated prior sessions), reused throughout, never duplicated.
- [x] 1.3 Read all project documentation and relevant .ai/skills, inspect the FE structure and complete existing Git changes, and identify reusable utilities, types, factories, hooks, components, and test seams; record a reuse map and baseline in the current session context and verify unrelated changes are preserved. `.ai/skills` has 3 skills (`nextjs-optimization`, `logic-n-1-optimize`, `nextjs-seo-optimization`); reviewed, none apply to a test-infrastructure change (image/font/SEO optimization, batch-API for production request loops — this change adds no production request loops). Reuse map lives in `docs/testing.md`'s "Known Jest/SWC limitation" note and the per-file behavior matrices; `git diff --stat` reconfirmed only the expected 16 files changed.
- [x] 1.4 Inspect GitNexus freshness and use query/context to understand affected flows; refresh a stale index while preserving existing embeddings. Before editing any existing function/class/method, run upstream impact and report direct callers, affected processes, and risk; verify HIGH/CRITICAL warnings are surfaced and d=1 dependents are accounted for. Use graph-aware rename/refactor procedures if applicable. No existing symbol was modified (additive-only change), so no rename/impact-before-edit applies; ran `gitnexus_detect_changes({scope:"all"})` (risk `low`, 0 affected processes) and `gitnexus_context({name:"useCourseOutlineReorder"})` (confirms only the expected 2 callers: `useCourseEditorState` and the test helper `renderReorderHook`) to verify the graph reflects the final implementation with no surprises.
- [x] 1.5 If research subagents are useful, apply AGENTS.md model/cost limits and provide the exact Session ID file, ID, and current context path before project access; verify each agent reuses that identity and only the main agent consolidates context unless a serialized edit is explicitly delegated. Record not applicable when no research delegation is needed. **Not applicable** — no research subagents were spawned at any point in this session; all research and implementation were done directly.
- [x] 1.6 Record the complete design behavior matrix, source-contract references, test layer, planned file placement, stage ownership, and residual backlog in the current session context; verify every spec requirement maps to an acceptance row and settle ambiguous expectations before asserting them. Reuse equivalent resources and create shared helpers only when none exist; write implementation work in English. The full behavior matrix lives in `docs/testing.md` (Stage 1/2/3 tables) and is referenced from `.context/session-8ac1e19a-....md`'s Final State section rather than duplicated there.
- [x] 1.7 Install compatible Jest, next/jest support dependencies, React Testing Library, jest-dom, user-event, and MSW development dependencies and update the lockfile; verify a clean `npm ci` succeeds under the CI Node version.
- [x] 1.8 Configure Node/jsdom test environments, existing aliases, source-colocated discovery, and explicit Jest imports; verify one real pure helper test and one real component test execute without browser provisioning and are type-checked by the existing build.
- [x] 1.9 Add reusable fixtures and render helpers with isolated locale/auth/SWR state and HTTP interception; verify state resets across consecutive cases and an unexpected request fails without contacting external services.
- [x] 1.10 Add refresh eligibility and token/envelope validation tests; verify the matrix covers 401/403, expiry headers, bearer presence, retry guards, valid rotation, and malformed/missing fields using table-driven cases.
- [x] 1.11 Add permission and navigation-tree tests; verify all/any/empty requirements, missing permissions, nested branch retention/removal, and independence from display-only role labels.
- [x] 1.12 Replace the placeholder `test` command and add watch/coverage commands while preserving every existing aggregate check; verify nonzero exit with a temporary failing assertion and with an empty test selection, remove the temporary probe, then verify the passing suite.
- [x] 1.13 Collect selected-scope coverage including zero-test files within the declared source targets and record its baseline in the current session context; verify the report and documentation do not claim exhaustive repository coverage or enforce an invented global percentage.
- [x] 1.14 Force-sync the FE GitNexus index after implementation using npx gitnexus analyze --force (add --embeddings when the existing index has embeddings); verify successful completion and that the index reflects the current implementation before running final quality gates.
- [x] 1.15 Run the Jest suite twice from reset test state for the isolation acceptance check, then npm run check-all; fix FE failures, including failures that would break FE CI, without changing BE or weakening protected tooling, and rerun required gates after fixes. Verify all recorded commands actually exit successfully; re-sync GitNexus if fixes changed indexed code.
- [x] 1.16 Review every file written or modified in this stage for architecture, placement, reuse, duplicate logic/types, unrelated changes, and CI readiness; inspect secrets only on the verified committable changed surface. Verify review findings are resolved, equivalent implementations are reused or merged, and any corrective edits repeat sync and required gates. Never scan ignored credentials or external session files.
- [x] 1.17 Reread all relevant project documentation, the complete .context folder, current session context, and GitNexus/project documents, then scan source and reconcile documentation with the final implementation. Rewrite outdated descriptions, add missing implemented behavior, remove nonexistent behavior, and synchronize relevant FE docs/GitNexus files; verify docs/testing.md describes only delivered functionality, preserves existing document languages, and links passing matrix rows plus remaining coverage. Do not merely append historical notes.
- [x] 1.18 Run `gitnexus_detect_changes` for the final changed scope, validate this OpenSpec change with --strict, and verify only expected files/flows changed, every modified existing symbol has pre-edit impact evidence, and all d=1 dependents are addressed. Validate documentation edits and rerun affected gates when final edits warrant them; leave any blocked check explicitly incomplete.
- [x] 1.19 Update only `.context/session-<SESSION_ID>.md` with the consolidated final state and the sections What I Learned Today and What I Should Learn or Do Next Time; verify the external ID still matches, exactly one project session file exists, no other session was edited, and coding state was not written to an external review context. **Done retroactively, consolidated rather than per-stage**: `.context/session-8ac1e19a-e093-478b-b3e1-41d3e67ffda0.md` covers all three stages' final state plus both required sections in one file (the per-stage progressive-write ceremony was skipped under the original hard time budget; the required deliverable content itself is now complete and current).
- [x] 1.20 Accept stage 1 only after all preceding stage tasks and required evidence are complete; report changed files, reused resources, deduplication, docs/GitNexus/context paths, actual passing/failing commands and resolutions, and remaining risks or hosted-CI checks. Perform the applicable memory persistence under governing instructions before the final response; do not claim unperformed writes or checks. Commit only when separately requested, with pre-commit change detection, the required hooks/message format, and post-commit reindexing preserving embeddings.

## 2. Stage 2 — Priority business flows

- [x] 2.1 Resume using the same external Session ID and exact project context file; reread the complete .context folder, project documentation, relevant skills, current Git changes, and GitNexus context before this stage. Verify the preceding stage is accepted, refresh stale graph data with embeddings preserved, and update the reuse map. Resumed under the same `AI_SESSION_ID`/`.context` file across every later turn in this session (verified matching, never duplicated); GitNexus force-synced and re-verified (`detect_changes` risk `low`) multiple times, most recently after Stage 3.
- [x] 2.2 Before this stage modifies any existing function/class/method, run and report upstream impact, address HIGH/CRITICAL warnings and direct dependents, and follow graph-aware refactoring rules; verify the planned edits remain FE-scoped and research subagents, if used, receive the same validated session identity. **Not applicable** — no existing production symbol was modified in Stage 2 (only additive test/config files and two additive `next.config.ts`/`eslint.config.mjs` entries), reconfirmed via `gitnexus_detect_changes` (0 affected processes); no research subagents were spawned.
- [x] 2.3 Add auth transport refresh/retry integration tests using the real transport with controlled HTTP responses; verify success, denial, malformed responses, bounded retry, and documented concurrent refresh coordination against the matrix.
- [~] 2.4 Add server refresh-route and cookie/session tests in the Node environment; verify successful rotation, response shape, and failure/clearing behavior against the existing contract without exposing real token values. **Deferred to Stage 3, not skipped silently**: confirmed by direct experiment that `cookies()` from `next/headers` throws ("called outside a request scope") when the route's exported `POST()` is invoked directly under Jest — it needs a live Next.js request scope. Covered instead by the Stage 3 browser auth journey against a real running Next server (see `docs/testing.md`).
- [x] 2.5 Add client auth lifecycle and permission UI tests; verify login/logout/session failure and visible allowed/denied navigation with actual state updates and isolated providers.
- [x] 2.6 Test the collaborator action and shared bulk finalizer; verify empty selection, one request containing all selected IDs and `EDITOR`, all/partial/no successes, transport failure, and loading cleanup.
- [x] 2.7 Add a consuming collaborator-picker integration test; verify partial success removes successful selections, retains failed selections, and displays the accepted summary instead of asserting service calls alone.
- [x] 2.8 Add table-driven outline reorder tests for sections, lessons, and sublessons; verify optimistic state, success merge, lease denial/rejection, persist rejection, rollback, and lease cleanup against the accepted contract. Record any contract discrepancy and resolve it explicitly before accepting this task. No contract discrepancy found.
- [x] 2.9 Add shared query integration tests; verify disabled/loading/empty/error/success states, pagination merging/deduplication, and prevention of duplicate load-more requests while pending.
- [~] 2.10 Add representative form interaction coverage through accessible controls; verify validation errors, pending submit behavior, server error presentation, and successful completion against documented behavior. **Validation errors and accessible-control coverage delivered**; server-error presentation and successful completion for this specific form are a Server Action outcome — same `next/headers` boundary as 2.4, deferred to the Stage 3 browser auth journey (documented in the test file and `docs/testing.md`).
- [x] 2.11 Force-sync the FE GitNexus index after implementation using npx gitnexus analyze --force (add --embeddings when the existing index has embeddings); verify successful completion and that the index reflects the current implementation before running final quality gates.
- [x] 2.12 Run the combined stage 1/2 Jest suite twice from reset test state for the isolation acceptance check, then npm run check-all; fix FE failures, including failures that would break FE CI, without changing BE or weakening protected tooling, and rerun required gates after fixes. Verify all recorded commands actually exit successfully; re-sync GitNexus if fixes changed indexed code.
- [x] 2.13 Review every file written or modified in this stage for architecture, placement, reuse, duplicate logic/types, unrelated changes, and CI readiness; inspect secrets only on the verified committable changed surface. Verify review findings are resolved, equivalent implementations are reused or merged, and any corrective edits repeat sync and required gates. Never scan ignored credentials or external session files. Found and fixed one real duplicate-logic issue (two clone pairs in `use-course-outline-reorder.test.tsx`); extracted a shared `renderReorderHook` helper, reran sync and gates.
- [x] 2.14 Reread all relevant project documentation, the complete .context folder, current session context, and GitNexus/project documents, then scan source and reconcile documentation with the final implementation. Rewrite outdated descriptions, add missing implemented behavior, remove nonexistent behavior, and synchronize relevant FE docs/GitNexus files; verify docs/testing.md describes only delivered functionality, preserves existing document languages, and links passing matrix rows plus remaining coverage. Do not merely append historical notes.
- [x] 2.15 Run `gitnexus_detect_changes` for the final changed scope, validate this OpenSpec change with --strict, and verify only expected files/flows changed, every modified existing symbol has pre-edit impact evidence, and all d=1 dependents are addressed. Validate documentation edits and rerun affected gates when final edits warrant them; leave any blocked check explicitly incomplete.
- [x] 2.16 Update only `.context/session-<SESSION_ID>.md` with the consolidated final state and the sections What I Learned Today and What I Should Learn or Do Next Time; verify the external ID still matches, exactly one project session file exists, no other session was edited, and coding state was not written to an external review context. **Done, consolidated with 1.19/3.17** — see 1.19.
- [x] 2.17 Accept stage 2 only after all preceding stage tasks and required evidence are complete; report changed files, reused resources, deduplication, docs/GitNexus/context paths, actual passing/failing commands and resolutions, and remaining risks or hosted-CI checks. Perform the applicable memory persistence under governing instructions before the final response; do not claim unperformed writes or checks. Commit only when separately requested, with pre-commit change detection, the required hooks/message format, and post-commit reindexing preserving embeddings.

## 3. Stage 3 — Browser runtime and bounded expansion

- [x] 3.1 Resume using the same external Session ID and exact project context file; reread the complete .context folder, project documentation, relevant skills, current Git changes, and GitNexus context before this stage. Verify the preceding stage is accepted, refresh stale graph data with embeddings preserved, and update the reuse map. Same resumed session as 2.1; re-verified again in the follow-up passes that added outline-reorder browser coverage and recovered `src/test-support/` after an environment-level file-loss incident (documented in `.context/session-8ac1e19a-....md`).
- [x] 3.2 Before this stage modifies any existing function/class/method, run and report upstream impact, address HIGH/CRITICAL warnings and direct dependents, and follow graph-aware refactoring rules; verify the planned edits remain FE-scoped and research subagents, if used, receive the same validated session identity. **Not applicable** — no existing production symbol was modified in Stage 3 (only additive test/e2e/config files), reconfirmed via `gitnexus_detect_changes` (0 affected processes) and `gitnexus_context({name:"useCourseOutlineReorder"})` (only expected callers); no research subagents were spawned.
- [x] 3.3 Install Playwright as a development dependency, update the lockfile, provision Chromium, and add browser configuration plus a loopback HTTP fixture backend with synthetic sessions and resettable scenarios; verify both browser and Next-server requests reach fixtures, unconfigured requests fail, and fixture URLs are supplied at build and runtime. Required a CORS fix on the fixture backend (explicit `Access-Control-Allow-Origin`/`-Credentials` + `OPTIONS` handling) discovered via direct experiment — documented in `docs/testing.md`.
- [x] 3.4 Add the `test:e2e` process lifecycle for the fixture backend and production-built Next application; verify readiness checks, fresh browser contexts, serial fixture reset, and process cleanup after both passing and intentionally failing temporary probes. Verified cleanup on both a failing run (pre-CORS-fix) and a passing run.
- [x] 3.5 Add browser auth journeys; verify login to a protected screen, logout, expired-session handling, and forbidden-response presentation through real route navigation and cookie round trips.
- [x] 3.6 Add the bounded course browser journey; verify collaborator submission and outline reorder success/failure recovery through visible UI state and the fixture backend's recorded requests. **Collaborator submission delivered** through the real UI. **Outline section reorder delivered** through `@dnd-kit`'s keyboard sensor (Space to pick up, ArrowDown to move, Space to drop) against two fixture sections wired to real `sections/reorder`/`leases/*` fixture endpoints; verified the visual reorder, the persisted-order toast, and the order surviving a page reload. Lesson/sub-lesson reorder and pointer-gesture simulation remain uncovered at the browser layer; the full reorder contract (including those) is already covered at the hook level in Stage 2.
- [x] 3.7 Add locale/404 browser cases covering valid locale, unknown route, and unsupported locale; verify accepted page content, provider composition, usable recovery navigation, and absence of unhandled rendering errors.
- [x] 3.8 Add instructor application-state and taxonomy validation tests; verify selected permission/status combinations and valid/invalid schema cases against their existing contracts.
- [x] 3.9 Add media caller tests with synthetic files; verify multipart contents, default/explicit visibility, accepted response handling, and error propagation without a real upload service.
- [x] 3.10 Add inbound event normalization tests; verify allowed source/type combinations, invalid payload rejection, fallback source, and deterministic default/preserved metadata without starting live transports.
- [x] 3.11 Add browser CI execution on pull requests and dev pushes with existing quality checks, fixture-configured build, and failure artifacts; verify workflow structure and command failures propagate, preserve the existing deployment workflow, and report hosted CI verification as pending until a real run exists. `.github/workflows/e2e-browser.yml`; `deploy-dev.yml`/`enforce-main-from-dev.yml` untouched; hosted-run verification explicitly reported pending in `docs/testing.md`.
- [x] 3.12 Force-sync the FE GitNexus index after implementation using npx gitnexus analyze --force (add --embeddings when the existing index has embeddings); verify successful completion and that the index reflects the current implementation before running final quality gates.
- [x] 3.13 Run the complete Jest and browser suites twice from reset test state for the isolation acceptance check, then npm run check-all; fix FE failures, including failures that would break FE CI, without changing BE or weakening protected tooling, and rerun required gates after fixes. Verify all recorded commands actually exit successfully; re-sync GitNexus if fixes changed indexed code. Jest: 2 consecutive clean runs (34 Node + 93 jsdom). Playwright: 2 consecutive clean runs (8/8).
- [x] 3.14 Review every file written or modified in this stage for architecture, placement, reuse, duplicate logic/types, unrelated changes, and CI readiness; inspect secrets only on the verified committable changed surface. Verify review findings are resolved, equivalent implementations are reused or merged, and any corrective edits repeat sync and required gates. Never scan ignored credentials or external session files. Only synthetic test accounts/tokens found in fixture/spec files; no real secrets.
- [x] 3.15 Reread all relevant project documentation, the complete .context folder, current session context, and GitNexus/project documents, then scan source and reconcile documentation with the final implementation. Rewrite outdated descriptions, add missing implemented behavior, remove nonexistent behavior, and synchronize relevant FE docs/GitNexus files; verify docs/testing.md describes only delivered functionality, preserves existing document languages, and links passing matrix rows plus remaining coverage. Do not merely append historical notes.
- [x] 3.16 Run `gitnexus_detect_changes` for the final changed scope, validate this OpenSpec change with --strict, and verify only expected files/flows changed, every modified existing symbol has pre-edit impact evidence, and all d=1 dependents are addressed. Validate documentation edits and rerun affected gates when final edits warrant them; leave any blocked check explicitly incomplete.
- [x] 3.17 Update only `.context/session-<SESSION_ID>.md` with the consolidated final state and the sections What I Learned Today and What I Should Learn or Do Next Time; verify the external ID still matches, exactly one project session file exists, no other session was edited, and coding state was not written to an external review context. **Done, consolidated with 1.19/2.16** — see 1.19; also documents the outline-reorder follow-up and the `src/test-support/` recovery incident.
- [x] 3.18 Accept stage 3 only after all preceding stage tasks and required evidence are complete; report changed files, reused resources, deduplication, docs/GitNexus/context paths, actual passing/failing commands and resolutions, and remaining risks or hosted-CI checks. Perform the applicable memory persistence under governing instructions before the final response; do not claim unperformed writes or checks. Commit only when separately requested, with pre-commit change detection, the required hooks/message format, and post-commit reindexing preserving embeddings.
